'use client';import Link from 'next/link';import { usePathname } from 'next/navigation';import { Menu } from 'lucide-react';import { nav } from '@/content/site';import { Button } from '@/components/ui/Button';import { Container } from '@/components/layout/Container';
export function Header(){const p=usePathname();return <header className="fixed inset-x-0 top-0 z-50 border-b border-border/70 bg-white/90 backdrop-blur-xl"><Container><div className="flex h-[72px] items-center justify-between gap-4 lg:h-[88px]"><Link href="/" className="text-2xl font-black text-primary">CHECKUP</Link><nav className="hidden items-center gap-5 text-sm font-medium lg:flex">{nav.map(([href,label])=><Link key={href} href={href} className={p===href?'font-black text-primary':'text-text-secondary hover:text-primary'}>{label}</Link>)}</nav><div className="hidden md:block"><Button href="/contact">درخواست جلسه آشنایی</Button></div><button className="lg:hidden" aria-label="باز کردن منو"><Menu/></button></div></Container></header>}
