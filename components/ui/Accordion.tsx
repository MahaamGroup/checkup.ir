'use client';import { useState } from 'react';import { ChevronDown } from 'lucide-react';import type { FaqItem } from '@/types/content';import { cn } from '@/lib/utils';
export function Accordion({items}:{items:FaqItem[]}){const[open,setOpen]=useState(0);return <div className="space-y-4">{items.map((item,i)=><div className="rounded-2xl border border-border bg-white" key={item.question}><button className="focus-ring flex w-full items-center justify-between p-5 text-right font-bold" onClick={()=>setOpen(open===i?-1:i)}><span>{item.question}</span><ChevronDown className={cn('transition',open===i&&'rotate-180')} /></button>{open===i&&<p className="px-5 pb-5 leading-8 text-text-secondary">{item.answer}</p>}</div>)}</div>}
