import Link from 'next/link';import { cn } from '@/lib/utils';
type Props={children:React.ReactNode;href?:string;variant?:'primary'|'secondary'|'ghost';className?:string;type?:'button'|'submit';disabled?:boolean};
export function Button({children,href,variant='primary',className,type='button',disabled}:Props){const cls=cn('focus-ring inline-flex h-11 md:h-12 items-center justify-center rounded-xl px-5 text-sm font-bold transition duration-200 hover:-translate-y-0.5',variant==='primary'&&'bg-primary text-white shadow-small hover:bg-primary-hover hover:shadow-medium',variant==='secondary'&&'border border-border bg-white text-text-primary hover:border-primary hover:text-primary',variant==='ghost'&&'text-primary hover:bg-brand-soft',disabled&&'pointer-events-none opacity-60',className);return href?<Link className={cls} href={href}>{children}</Link>:<button className={cls} type={type} disabled={disabled}>{children}</button>}
