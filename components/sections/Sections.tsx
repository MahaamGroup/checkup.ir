import { Container } from '@/components/layout/Container';import { Section } from '@/components/layout/Section';import { SectionTitle } from '@/components/typography/SectionTitle';import { Button } from '@/components/ui/Button';import { BaseCard } from '@/components/cards/BaseCard';import { Accordion } from '@/components/ui/Accordion';import type { CardItem,FaqItem,HeroContent } from '@/types/content';import { cta } from '@/content/site';import { ProcessDiagram } from '@/components/diagrams/ProcessDiagram';import { AssessmentDiagram } from '@/components/diagrams/AssessmentDiagram';import { OpportunityDiagram } from '@/components/diagrams/OpportunityDiagram';import { FrameworkDiagram } from '@/components/diagrams/FrameworkDiagram';
export function Hero({content}:{content:HeroContent}){const centered=content.variant==='centered';return <Section className="overflow-hidden bg-gradient-to-br from-white to-brand-soft"><Container className={centered?'text-center':'grid items-center gap-12 xl:grid-cols-2'}><div className={centered?'mx-auto max-w-3xl':''}><p className="mb-4 font-bold text-primary">{content.eyebrow}</p><h1 className="text-4xl font-black leading-tight text-text-primary md:text-6xl">{content.title}</h1><p className="mt-6 max-w-[72ch] text-lg leading-9 text-text-secondary">{content.subtitle}</p><div className="mt-8 flex flex-wrap gap-4"><Button href={content.primary?.href??'/contact'}>{content.primary?.label??'درخواست جلسه آشنایی'}</Button>{content.secondary&&<Button href={content.secondary.href} variant="secondary">{content.secondary.label}</Button>}</div></div>{!centered&&<div className="rounded-3xl border border-border bg-white p-8 shadow-large"><FrameworkDiagram/></div>}</Container></Section>}
export function CardGridSection({eyebrow,title,description,items,tint=false,cols='3'}:{eyebrow?:string;title:string;description?:string;items:CardItem[];tint?:boolean;cols?:'2'|'3'|'4'}){return <Section tint={tint}><Container><SectionTitle eyebrow={eyebrow} title={title} description={description}/><div className={`grid gap-6 md:grid-cols-2 ${cols==='4'?'xl:grid-cols-4':'xl:grid-cols-3'}`}>{items.map(item=><BaseCard item={item} key={item.title}/>)}</div></Container></Section>}
export function TextSection({eyebrow,title,description,tint=false}:{eyebrow?:string;title:string;description:string;tint?:boolean}){return <Section tint={tint}><Container><SectionTitle eyebrow={eyebrow} title={title} description={description}/></Container></Section>}
export function DiagramSection({type,title,description}:{type:'process'|'assessment'|'opportunity'|'framework';title:string;description:string}){const D={process:<ProcessDiagram/>,assessment:<AssessmentDiagram/>,opportunity:<OpportunityDiagram/>,framework:<FrameworkDiagram/>}[type];return <Section tint><Container><SectionTitle title={title} description={description}/><div className="rounded-3xl border border-border bg-white p-6 shadow-small">{D}</div></Container></Section>}
export function FAQSection({items}:{items:FaqItem[]}){return <Section><Container><SectionTitle title="پرسش‌های متداول" description="پاسخ به پرسش‌های رایج درباره ماهیت همکاری و خروجی‌ها."/><Accordion items={items}/></Container></Section>}
export function CTASection(){return <Section className="bg-brand-soft"><Container><div className="rounded-3xl bg-white p-8 text-center shadow-medium md:p-12"><h2 className="text-3xl font-black">{cta.title}</h2><p className="mx-auto mt-4 max-w-[72ch] leading-8 text-text-secondary">{cta.description}</p><div className="mt-8 flex justify-center gap-4"><Button href={cta.primary.href}>{cta.primary.label}</Button><Button href={cta.secondary.href} variant="secondary">{cta.secondary.label}</Button></div></div></Container></Section>}
export function ReadingSection({title}:{title:string}){return <Section><Container className="max-w-4xl"><div className="prose-checkup"><h2>{title}</h2><p>این صفحه برای ارائه اطلاعات شفاف، قابل نگهداری و سازگار با نسخه نخست وب‌سایت CHECKUP تدوین شده است. متن حقوقی نهایی می‌تواند در مرحله بهره‌برداری توسط تیم حقوقی تکمیل شود.</p><p>تمام تعاملات وب‌سایت با هدف معرفی خدمات مدیریتی، ایجاد اعتماد و تسهیل درخواست جلسه آشنایی طراحی شده‌اند و هیچ خدمت پزشکی یا نوبت‌دهی درمانی ارائه نمی‌شود.</p></div></Container></Section>}
