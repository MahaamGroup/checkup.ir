import type { CardItem } from '@/types/content';import { IconFrame } from '@/components/icons/IconFrame';import { cn } from '@/lib/utils';
export function BaseCard({item,className,children}:{item:CardItem;className?:string;children?:React.ReactNode}){return <article className={cn('group h-full rounded-2xl border border-border bg-surface p-6 shadow-small transition duration-250 hover:-translate-y-1 hover:shadow-medium',className)}>{item.icon&&<IconFrame icon={item.icon}/>} {item.badge&&<span className="mb-3 inline-flex rounded-full bg-brand-soft px-3 py-1 text-xs font-bold text-primary">{item.badge}</span>}<h3 className="text-xl font-extrabold text-text-primary">{item.title}</h3><p className="mt-3 leading-8 text-text-secondary">{item.description}</p>{children}</article>}
