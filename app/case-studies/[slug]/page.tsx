import { notFound } from 'next/navigation';import { CardGridSection, CTASection, DiagramSection, Hero, TextSection } from '@/components/sections/Sections';import { cases } from '@/content/site';
export function generateStaticParams(){return cases.map(i=>({slug:i.slug}))}
export default async function Page({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const item=cases.find(i=>i.slug===slug);if(!item)notFound();return <><Hero content={{variant:'compact',eyebrow:item.category,title:item.title,subtitle:item.description}}/><TextSection title="چالش" description="سازمان با ابهام در اولویت‌های مدیریتی، ناهماهنگی اجرایی و نبود شاخص‌های تصمیم‌ساز مواجه بود."/><TextSection tint title="رویکرد" description="ابتدا تشخیص مدیریتی انجام شد، سپس فرصت‌ها اولویت‌بندی و نقشه اقدام طراحی شد."/><DiagramSection type="framework" title="چارچوب استفاده‌شده" description="چارچوب تشخیص تا پایش برای اتصال تحلیل به اقدام."/><TextSection title="نتیجه" description="خروجی نهایی، مسیر روشن تصمیم‌گیری و برنامه اقدام قابل پیگیری برای مدیران ارشد بود."/><CardGridSection tint title="مطالعات مرتبط" items={cases.filter(i=>i.slug!==slug).slice(0,2)}/><CTASection/></>}
